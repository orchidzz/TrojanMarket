export const initialUserState = {
    userImg: null,
    userName: null,
    listedItems: [],
    loading: false,
    error: null,
};

export const initialAuthState = {
    token: null,
    userEmail: null,
    loading: false,
    error: null,
};

export const initialItemsState = {
    items: [],
    loading: false,
    error: null,
};
